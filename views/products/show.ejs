<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= product.name %>
  </title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>



  <div class="flex flex-row justify-center p-40 items-center">
    <div class=" max-w-sm p-6 bg-white border border-gray-200 rounded-lg shadow dark:bg-gray-800 dark:border-gray-700">
      <a href="#">
        <h5 class="mb-2 text-2xl font-bold tracking-tight text-gray-900 dark:text-white"><a
            href="/products/<%= product._id %>">
            Name : <%= product.name %>
          </a></h5>
      </a>
      <p class="mb-3 font-normal text-gray-700 dark:text-gray-200">
        Price : <%= product.price %>₺
      </p>
      <p class="mb-3 font-normal text-gray-700 dark:text-blue-400">
        Category : <a href="/products?category=<%=product.category %>" class="text-blue"><%=product.category %></a>
      </p>
      <div class="inline-flex">
        <button class="bg-gray-50 hover:bg-gray-400 text-gray-800 font-bold py-2 px-4 rounded-l">
          <a href="/products">Back to All Products</a>
        </button>
        <button class="bg-gray-50 hover:bg-gray-400 text-gray-800 font-bold py-2 px-4 rounded-r">
          <a href="/products/<%= product._id %>/edit">Edit Product</a>
        </button>
      </div>
      <form action="/products/<%= product._id %>?_method=DELETE" method="POST" class="flex flex-row justify-center pt-4">
        <button class="text-white bg-gradient-to-r from-red-400 via-red-500 to-red-600 hover:bg-gradient-to-br focus:ring-4 focus:outline-none focus:ring-red-300 dark:focus:ring-red-800 shadow-lg shadow-red-500/50 dark:shadow-lg dark:shadow-red-800/80 font-medium rounded-lg text-sm px-5 py-2.5 text-center mr-2 mb-2">Delete</button>
      </form>
    </div>
  </div>

</body>

</html>